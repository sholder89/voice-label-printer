# Copy this to .env in the server/ directory before running docker compose
LABEL_TOKEN=change-me-to-something-secret

# Optional tuning
MAX_PENDING_JOBS=20   # reject new jobs if this many are already queued
MAX_TEXT_LEN=200      # reject label text longer than this many characters
